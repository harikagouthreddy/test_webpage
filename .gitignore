# Kitchen cache
**/.kitchen

# TF Cache
**/.terraform

# A private tfvars file
priv.tfvars
priv.tfvars.json

# Crash log files
crash.log
crash.*.log

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Plans can contain secrets so we do not commit then
**/*tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix
